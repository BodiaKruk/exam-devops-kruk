apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-exam-release
  namespace: release
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-exam
  template:
    metadata:
      labels:
        app: devops-exam
    spec:
      containers:
      - name: devops-exam
        image: <DOCKERHUB_USER>/devops-exam:1.0.0
        ports:
        - containerPort: 8003
---
apiVersion: v1
kind: Service
metadata:
  name: devops-exam-release
  namespace: release
spec:
  type: NodePort
  selector:
    app: devops-exam
  ports:
    - port: 8003
      targetPort: 8003
      nodePort: 30083
